**NB:** What started as a CLI script is being re-built into a flask app.  Work still in progress. Basic functionality still works if you clone the repro and run `flask run` to boot it up on localhost.  
